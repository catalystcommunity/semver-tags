name: test-go
description: "Build, vet, and test semver-tags through the runnerlib lifecycle"
job:
  image: containers.catalystsquad.com/public/reactorcide/runnerbase:latest
  command: "runnerlib run --job-command true"
  timeout: 1800
  priority: 10
  working_dir: /job
environment:
  SEMVER_TAGS_CI_JOB: test-go
run_local:
  user: host
